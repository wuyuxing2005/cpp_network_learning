## 启动server方法
命令行运行make,之后等待编译完成.
然后./server启动http服务器

## 压力测试：（如果要在wsl上测试的话需要你调小并发数，或者修改你的wsl的临时端口）
需要先安装ab或者wrk

//表示在10000个并发下以共进行20000次请求
ab -k -n 20000 -c 10000 http://127.0.0.1:9999/
//表示在10000个并发下保持连接30s
wrk -t8 -c10000 -d30s --latency -H "Connection: keep-alive" http://127.0.0.1:9999/